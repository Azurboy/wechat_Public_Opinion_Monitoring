# 舆情监测关键词配置示例
# 复制此文件为 keywords.yaml 并修改

keywords:
  - 你的关键词1
  - 你的关键词2

# 搜索设置
search:
  # 每个关键词搜索的页数
  max_pages: 1
  # 请求间隔（秒）
  request_delay: 3
  # 是否按时间排序
  sort_by_time: true

# 关联词过滤（关键词必须同时包含这些词之一才保留）
# 例如：搜索"Monolith"时，只保留同时包含"投资"或"基金"等词的文章
relevance_keywords:
  关键词1:
    - 关联词1
    - 关联词2
    - 关联词3
  关键词2:
    - 关联词1
    - 关联词2
